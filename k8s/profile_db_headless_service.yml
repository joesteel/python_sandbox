apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  clusterIP: None  # <-- This makes it headless!
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432